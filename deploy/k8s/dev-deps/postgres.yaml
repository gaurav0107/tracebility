---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: postgres
  namespace: langprobe
  labels:
    app.kubernetes.io/name: postgres
    app.kubernetes.io/component: dev-deps
spec:
  replicas: 1
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app.kubernetes.io/name: postgres
  template:
    metadata:
      labels:
        app.kubernetes.io/name: postgres
        app.kubernetes.io/component: dev-deps
    spec:
      containers:
        - name: postgres
          image: postgres:16
          env:
            - name: POSTGRES_DB
              value: langprobe
            - name: POSTGRES_USER
              value: langprobe
            # Password is sourced from a Secret the operator generates at
            # bootstrap (openssl rand) — never a committed default. See
            # deploy/k8s/dev-deps/README.md and the GKE bootstrap runbook.
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: langprobe-db-bootstrap
                  key: postgres_password
            - name: PGDATA
              value: /var/lib/postgresql/data/pgdata
          ports:
            - containerPort: 5432
              name: pg
          volumeMounts:
            - name: data
              mountPath: /var/lib/postgresql/data
          readinessProbe:
            exec:
              command: ["pg_isready", "-U", "langprobe", "-d", "langprobe"]
            initialDelaySeconds: 5
            periodSeconds: 5
          resources:
            requests:
              cpu: 100m
              memory: 256Mi
            limits:
              cpu: 1000m
              memory: 1Gi
      volumes:
        - name: data
          emptyDir: {}
---
apiVersion: v1
kind: Service
metadata:
  name: postgres
  namespace: langprobe
spec:
  selector:
    app.kubernetes.io/name: postgres
  ports:
    - name: pg
      port: 5432
      targetPort: pg
